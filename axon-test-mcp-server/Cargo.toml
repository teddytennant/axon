[package]
name = "axon-test-mcp-server"
version = "0.1.0"
edition = "2021"
publish = false
description = "Minimal MCP server for integration testing"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
